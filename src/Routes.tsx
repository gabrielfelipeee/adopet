import { BrowserRouter } from "react-router-dom"

import Header from "./components/Header";

function Routes() {
  return (
    <BrowserRouter>
      <Header />
    </BrowserRouter>
  )
}
export default Routes;
